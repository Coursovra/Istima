using UnityEngine;

public class StartGamePanelView : MonoBehaviour
{
    [SerializeField] private GameObject _player;
    
    private void Start()
    {
        //todo: 
        //transform.position = new Vector2(0, _player.transform.position.y);
    }
}
